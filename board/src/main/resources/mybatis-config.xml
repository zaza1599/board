<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd" >
<configuration>
<!-- Mybatis 관련 설정 작성하기 -->
	<!-- SqlSessionTemplate 관련 설정 -->
	<settings>
		<!-- 
	 		insert/update 진행 시 null이 포함되어 있는 경우
	 		Mybatis 기본적으로 애러를 발생 시킴.
	 		해당 구문이 작성되면 지정된 value로 값을 insert/update 시킴.
	 		
	 		* null 값이 있으면 NULL(무조건 대문자)을 insert/update 해라.
	 		
	 		-->
	
		<setting name="jdbcTypeForNull" value="NULL"/>
		
	</settings>
	
	<typeAliases>
		<typeAlias type="com.memberboard.board.board.model.vo.Board" alias="board"/>
	</typeAliases>
	
	<mappers>
	<mapper resource="/mappers/board-mapper.xml"/>
	
	</mappers>
</configuration>
